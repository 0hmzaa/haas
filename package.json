{
  "name": "human-as-a-service",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.7.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "demo:happy-path": "bash scripts/demo-happy-path.sh"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
